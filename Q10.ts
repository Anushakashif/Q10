// 10. Adding Comments: Choose two of the programs you've written, and add at least one comment to each . If you don't have anything specific to Write 
// because your programs are to simple at this point, just add your name and the current data at the top of each program File. Then write one sentence 
// describing what the program does. 

let a: number = 10
let b: number = 20

// Addition
console.log(`${a}+${b} = ${a + b}`);
// Subtraction
console.log(`${a}-${b} = ${a - b}`);
// Multiplication
console.log(`${a}*${b} = ${a * b}`);
// Division
console.log(`${a}/${b} = ${a / b}`);